## 赛道定位与能力考察重点
本赛道重点考察选手对 AI Agent 的深刻理解与驾驭能力，以及在多端复杂环境下的工程落地能力。我们鼓励具备以下能力与背景的选手参赛：
- 是 AI Native 的思考者，能将 Agent 思维融入产品设计，以 AI 驱动核心流程，而非简单堆砌 GUI。
- 具备扎实的客户端基础，能应对移动端与桌面端差异，保证多端体验的一致性与流畅性。
- 拥有卓越的工程与产品能力，能端到端交付高质量、高可用的复杂应用。
具体命题：Agent-Pilot · 从 IM 对话到演示稿的一键智能闭环
背景与痛点
在快节奏的团队协作中，一个需求往往始于一次 IM 对话（群聊/单聊均可），历经文档撰写、多方讨论、方案修改，最终沉淀为一份正式的演示文稿。这个过程充满了大量重复、繁琐的手动操作与跨应用切换，极大地消耗了团队的创造力。如果有一个智能助手，能理解对话意图，将这些步骤拆解为若干可重用的场景模块并按需编排自动串联起整个工作流，那将是生产力的巨大飞跃。
任务目标与范围
你需要设计并实现一个以 AI Agent 为核心的多端协同办公助手，它能驱动并串联 IM（即时通讯）、文档、演示文稿/自由画布 这三个核心办公套件，实现从需求捕捉到成果汇报的全链路自动化。
核心理念：AI Agent 是主驾驶（Pilot），GUI 界面是仪表盘与辅助操作台（Co-pilot）。用户通过自然语言（语音或文本）下达指令，Agent 负责理解、拆解任务并驱动各端应用完成核心操作，GUI 则用于展示进度、提供必要确认或进行精细调整。
- 必须完成项 (Must-have)：
  1. 多端协同框架：构建一套能在移动端（iOS 或 Android）和桌面端（macOS 或 Windows）之间实时同步状态与数据的框架。一端的操作能无缝体现在另一端。
  2. Agent 驱动的模块化场景与可组合编排：
  - 场景 A：意图/指令入口（IM 群聊/单聊均可，支持文本/语音），负责捕捉用户自然语言意图并启动任务。
  - 场景 B：任务理解与规划（LLM/Planner，不限实现），将用户意图拆解为可执行的子任务与步骤。
  - 场景 C：文档/白板生成与编辑，围绕需求自动生成并迭代核心文档或白板内容。
  - 场景 D：演示稿（PPT/自由画布）生成与排练，将已沉淀内容结构化为演示材料，并支持演练与修改。
  - 场景 E：多端协作与一致性（移动端+桌面端），保证跨端状态与内容的一致和冲突解决。
  - 场景 F：总结与交付（汇报、归档），输出面向汇报/归档的成果（如分享链接、导出文件等）。
  - 各场景可独立成立并单独演示，也可按需求自由组合编排。参赛作品需同时满足“AI Agent 为主、GUI 为辅”和“覆盖 IM、Doc，并包含 PPT 或自由画布至少一种”的要求，不限定固定触发顺序与链路形态。
  3. 自然语言交互：支持文本或语音指令，覆盖任务启动、进度查询、内容生成等关键节点。
- 可选加分项 (Good-to-have)：
  - 离线支持：在网络断开时，用户仍可在本地进行编辑，待网络恢复后数据能智能、无冲突地合并同步。
  - 高级 Agent 能力：Agent 能主动发起任务澄清、总结讨论、或根据上下文推荐下一步行动。
  - 富媒体与画布操作：支持在文档/画布中通过指令插入和处理图片、表格等，或进行布局调整。
  - 第三方平台集成：基于飞书或同类开放平台的 API 实现，展示对现有生态的利用与拓展能力。若选择从零开始，则考察更底层的架构设计能力。
演示与验收要求
你的最终演示必须清晰地展示以下场景。允许单场景独立演示，须至少展示一次多场景组合编排；IM 入口的群聊/单聊均可：
- 多端协同：在移动端与桌面端之间流畅切换，展示数据与状态的实时、双向同步。
- Agent 驱动主流程：演示选手如何通过 IM 入口（群聊/单聊均可）以自然语言触发任务，由 Agent 驱动意图理解、文档/白板生成、演示稿生成等多个场景模块的编排执行，而非依赖单一固定链路。
- Office 套件覆盖：演示中必须明确体现对 IM、文档、演示文稿/自由画布 三个组件的串联与操作。

## 解决方案
一、项目定位
这个项目肯定不能“重新写一个 Agent 框架”。赛题要求覆盖 IM、文档、演示稿或白板、多端协同、Agent 规划与执行，这些能力本身在飞书生态和现有 Agent 增强框架里已经有大量现成轮子。真正合理的落地点，是做一个面向飞书办公场景的 Agent-Pilot 编排壳。
这个编排壳的核心价值，不是自己造 IM、Doc、Slides、Whiteboard，也不是魔改 Codex、OpenCode、OpenClaw 或其他大型 Agent 框架的内部源码，而是把这些已经存在的能力组织成一个可演示、可运行、可解释的办公自动化闭环。用户在飞书 IM 里提出自然语言请求，Codex + superpowers 接管任务，自动完成规划、追问、拆解、执行、验收，然后调用飞书文档、飞书演示稿、白板、Presenton、lark-cli 等工具生成真实办公产物，最后再把结果同步回飞书。
所以这个项目的定位可以概括为：基于飞书开放生态和增强 Agent Harness 的办公协同自动化系统，实现从 IM 对话到文档、演示稿、白板和归档交付的一键闭环。
二、不要从零造办公套件
赛题看起来要求很多组件，但如果从零造轮子，基本不可行。IM 的消息体系、多端同步、权限、消息历史、群聊和单聊、文档协同编辑、PPT 编辑器、白板渲染、文件归档，这些东西都不是比赛周期内应该重做的内容。
飞书本身已经提供 IM、云文档、演示稿、白板、文件、知识库、多端同步等基础办公能力。飞书官方的 lark-cli 和 lark-openapi-mcp 也已经把很多能力暴露成命令行和 MCP 工具。Presenton 已经能从文本或文档生成 PPTX/PDF，并且提供 API 和 MCP。Codex 也有 slides skill，能辅助生成和检查 PPT。OpenClaw-Lark 已经具备飞书通道能力，可以作为入口参考或备选。
因此，项目的基本原则是：底层办公组件尽量复用飞书，PPT 生成尽量复用 Presenton 或 Codex Slides Skill，Agent 编排统一收敛到 Codex CLI + superpowers。我们自己只做连接、约束、任务投递、结果回传、场景包装和演示闭环；其他框架、服务或网关只能作为工具接入，不能成为主编排层。
三、Agent 编排层不建议自研
一开始容易误判，以为需要自己写一个 Python 工作流引擎，自己做任务规划、步骤执行、状态存储、失败恢复。现在编排层直接确定为 Codex CLI + superpowers：Codex 负责主代理执行、代码与文件操作、工具调用和本地验证；superpowers 负责把规划、TDD、调试、验收、子任务拆分、技能使用这些过程固化成可复用的方法论。
因此，这个项目不应该在 Python 里重写一套 workflow engine，也不再采用其他框架作为主编排方案。更合理的做法是：让 Codex + superpowers 作为唯一主编排层，Python Bridge 只负责 IM 入口、任务目录、状态协议、外部工具调用和结果回传。其他工具可以通过 CLI、HTTP、MCP 或 Gateway 挂到 Codex 可调用的工具面上。
四、推荐总体架构
整体架构应该是 Harness-first，也就是增强 Agent Harness 优先。
用户先在飞书 IM 中发起任务，比如“根据刚才群聊内容，生成项目方案和 8 页答辩 PPT”。飞书消息被一个轻量 Python Bridge 接收。Python Bridge 不负责复杂规划，只负责读取消息、收集必要上下文、创建任务目录，把任务写成一个 request.md 文件。然后 Bridge 调用 Codex CLI，让 Codex 在当前 workspace 中读取 request.md，并按项目内办公 Skill 与 superpowers 流程执行任务。
Codex + superpowers 在执行过程中负责追问、规划、分派、调用工具和验收。工具层包括 lark-cli、lark-openapi-mcp、Presenton、Codex Slides Skill、PptxGenJS 或其他 PPT 工具。执行完成后，Agent 必须写出一个 artifacts.json，里面记录文档链接、PPT 链接、白板链接、摘要、状态等信息。Python Bridge 监控这个文件，一旦发现任务完成，就把结果回传到飞书 IM。
整体链路可以概括为：飞书 IM 触发任务，Python Bridge 写入任务请求，Codex + superpowers 作为主 Agent Harness 执行，lark-cli 和 Presenton 作为工具层生成真实办公产物，最后 Python Bridge 把产物链接和任务状态回传飞书。
五、系统边界划分
系统里最核心的边界是：Python 不做 Agent 内核，Codex 不负责飞书 webhook，飞书不承担 Agent 编排，Presenton 不负责办公场景理解。
Python Bridge 负责入口和出口。它接收飞书消息，把消息变成任务文件，启动或投喂 Codex CLI，监控任务目录和产物文件，最后把结果发回飞书。
Codex + superpowers 负责主编排。它读取任务请求，提出澄清问题，生成任务计划，必要时使用子代理，调用办公工具，检查产物是否满足要求。
lark-cli 和 lark-openapi-mcp 负责飞书办公操作。比如读取群聊历史，创建飞书文档，更新文档内容，上传文件，创建或更新 Slides，生成白板内容，发送消息。
Presenton 负责 PPT 初稿生成。它可以根据文档大纲或 markdown 内容生成 PPTX/PDF，并提供可下载文件。
Codex Slides Skill 或 PptxGenJS 可以作为 PPT 精修和格式校验工具，用于检查页面溢出、布局、讲稿和结构。
飞书原生客户端负责最终用户界面。用户在手机端和桌面端都能看到 IM、文档、PPT、白板和交付结果。
六、为什么 Python 只做胶水层
用户当前对 Python 更熟，对 TS 生态和大型 Agent 框架源码没有维护经验。因此，不能选择“fork 一个大型 TS Agent 框架再魔改”的路线。Codex、OpenCode、OpenClaw、MCP Gateway 这些东西都可以用，但不应该深入源码修改；其中 Codex + superpowers 是唯一主编排层，其余只作为外部工具、服务或网关。
最合理的方式是把这些工具都当黑盒外部程序。Python 通过四种稳定边界与它们交互：命令行、HTTP、文件、JSON。比如用 subprocess 调 lark-cli，用 requests 调 Presenton，用文件系统向 Codex 投递任务，用 artifacts.json 接收最终结果。
这样做的好处是，项目复杂度被压低。即使不懂 TS，也可以控制任务流。出了问题，也可以通过任务目录、日志、tmux 输出和 artifacts.json 定位。项目的核心不再是改别人框架，而是写好任务模板、Skill 说明、工具适配、结果协议和演示流程。
七、推荐仓库形态
仓库应该是一个 Agent workspace 加上轻量 bridge，而不是一个传统全栈项目。
可以包含 AGENTS.md，用来描述整个项目的执行原则和办公场景目标。可以包含 skills 目录，里面放 feishu-office、presenton-slides、delivery-archive、whiteboard 等 Skill。可以包含 bridge 目录，放飞书 webhook、tmux 控制、任务写入、产物监听和飞书消息回传。可以包含 tasks 目录，每个任务一个 task_id 子目录，里面有 request.md、status.json、artifacts.json 和日志。可以包含 examples 目录，放演示群聊和测试任务。可以包含 scripts 目录，放 smoke test 和 demo 脚本。
一个任务目录的结构可以很简单：request.md 保存用户请求和群聊上下文，status.json 保存当前执行状态，artifacts.json 保存最终产物，tmux.log 保存 Agent 运行日志。Codex 不需要和 Python 深度耦合，只要遵守“读取 request.md，写出 artifacts.json”这个协议即可。
八、Skill 是核心资产
这个项目最重要的不是 Python 代码，而是办公场景 Skill。Skill 要告诉 Agent：你有什么工具，什么时候用这些工具，怎样判断任务完成，最终必须输出什么格式。
比如 feishu-office skill 应该写清楚：不能只在聊天框里回答内容，必须创建真实飞书文档或更新已有文档；如果任务要求 PPT，就必须生成真实 PPTX 或飞书 Slides 链接；如果任务涉及白板，就需要输出 Mermaid、流程图或飞书 Whiteboard 产物；每一步重要结果都要写入任务目录；缺少项目名、页数、汇报对象、风格等关键信息时，要先向用户澄清。
presenton-slides skill 应该写清楚：输入是 markdown 文档、页数、受众、汇报时长和风格；输出是 PPTX 文件、slides outline、speaker notes 和 rehearse questions；验收标准包括页数符合要求、结构完整、每页标题清晰、不要内容溢出、必须包含封面、痛点、方案、流程、成果和总结。
delivery-archive skill 应该写清楚：完成后必须生成交付摘要，列出文档链接、PPT 链接、白板链接、群聊决策摘要、修改记录和后续待办。
这些 Skill 才是真正把通用 Agent Harness 改造成“飞书办公协同 Agent”的关键。
九、tmux 的作用
tmux 可以作为 TUI 监视和半自动控制层。Codex 这类工具很多时候都以 TUI 或终端交互方式运行。Python 可以通过 tmux 创建 session，启动 agent，发送任务 prompt，抓取屏幕输出，保存日志。这样一方面可以自动化投递任务，另一方面可以让人类随时 attach 进去观察或救场。
但 tmux 不应该作为唯一稳定协议。tmux 能发按键，也能抓屏幕，但它不知道 TUI 什么时候真正完成响应，也不一定知道当前焦点在哪里，遇到选择框、确认框、pager、滚动输出时容易不稳定。因此，tmux 适合监视和兜底，不适合承担最终结果判断。
最终是否完成任务，不应该靠 capture-pane 识别屏幕文本，而应该靠文件协议判断，比如 artifacts.json 是否生成、status.json 是否变成 completed。换句话说，tmux 负责“看见 Agent 在干活”，文件系统负责“确定 Agent 干完了”。
如果后续要更稳定地控制 TUI，可以研究 tui-use 这类更懂 PTY 的工具。它能等待屏幕稳定或等待特定文本出现，比纯 tmux send-keys 更适合自动化控制。但 MVP 阶段，tmux 加文件协议已经足够。
十、Hermes、OpenAkita、ZeroClaw、Nanobot 的位置
Hermes、OpenAkita、ZeroClaw、Nanobot 这些框架都可以看成 OpenClaw-like 或轻量 Agent Gateway 方向的候选。它们的共同特点是试图提供更轻量的 Agent 网关、消息通道、工具调用或个人常驻 Agent 能力。
Hermes 更偏个人常驻 Agent 和多平台聊天网关，支持 Telegram、Discord、Slack、WhatsApp、Signal、CLI 等通道，但飞书不是它的一等能力。如果用它接飞书，仍然需要自己写 Feishu channel adapter。
OpenAkita 是 open-source AI assistant framework，强调 skills 和 agent architecture，但当前来看飞书生态并不是它的核心优势。
ZeroClaw 是 OpenClaw 替代方向，强调轻量、快速和 autonomous gateway，但是否有稳定飞书通道还需要进一步验证。如果没有现成 Feishu 插件，仍然需要写 adapter。
Nanobot 更轻，更适合阅读和理解，也支持 MCP 和聊天通道，但它同样不是现成的飞书办公闭环。
所以这些框架可以作为备选调研对象或外部 Gateway 候选，但不应该喧宾夺主。当前路线明确为 Codex + superpowers 做主编排，Python Bridge 做飞书入口，lark-cli 和 lark-openapi-mcp 做飞书工具，Presenton 做 PPT 生成。
十一、前端不建议再做 Jinja
这个项目不需要传统 Jinja 后台页面，也不需要再做大量悬浮圆角卡片式后台。赛题本身强调 GUI 是仪表盘和辅助操作台，核心交互发生在 IM 和办公套件里。因此，最自然的用户界面就是飞书原生界面。
用户在飞书 IM 里发起任务，在飞书文档里看方案，在飞书 Slides 或上传的 PPT 里看演示稿，在飞书白板里看流程图，在飞书消息里接收任务进度和产物链接。这已经是一个完整多端界面，因为飞书本身有手机端和桌面端。
如果需要开发者控制台，推荐用 Textual 做 Python TUI。Textual 可以做一个终端驾驶舱，左侧显示任务列表，中间显示 tmux/Agent 输出，右侧显示产物链接、状态、错误和确认项。它比 Jinja 更适合 Agent 运行监控，也更符合“驾驶舱”的感觉。
如果确实需要给评委一个简单网页，可以考虑 NiceGUI。它用 Python 写 Web UI，不需要 TS/React，也比 Jinja 更容易快速做交互页面。但这应该是可选层，不是主界面。
最终 UI 组合可以是：用户视角用飞书原生 IM、Doc、Slides、Whiteboard；开发者视角用 Textual TUI 监控 Agent；评委视角主要看飞书闭环，必要时辅以 NiceGUI 任务状态页。
十二、MVP 演示主线
最小演示主线应该围绕“从飞书群聊到项目方案和答辩 PPT”展开。
先准备一个飞书群聊，里面有几个人讨论项目背景、功能、创新点、展示重点和分工。用户在手机端飞书里 @Agent，说“根据刚才讨论，生成项目方案和 8 页答辩 PPT，并给出讲稿”。
Python Bridge 收到飞书消息后，收集最近群聊上下文，创建 task_id，把用户请求和群聊内容写入 tasks/<task_id>/request.md。然后它通过 tmux 或 headless CLI 调用 Codex，让 Agent 按照办公 Skill 和 superpowers 流程执行任务。
Codex 读取任务后，如果信息不足，可以先反问用户，比如“PPT 面向评委还是老师？”“汇报时长是 3 分钟还是 5 分钟？”用户回复后，Agent 继续执行。随后它调用 lark-cli 创建飞书文档，调用 Presenton 生成 PPTX 或 slides outline，必要时生成白板流程图，最后写出 artifacts.json。
Python Bridge 监控 artifacts.json，发现任务完成后，把文档链接、PPT 链接、讲稿摘要、白板链接和归档摘要发回飞书群。用户在桌面端打开飞书文档和 PPT，看到产物已经生成；用户再在手机端问“现在还缺什么”，Agent 根据任务状态返回剩余待办。
这条链路覆盖了 IM 入口、自然语言交互、Agent 规划与追问、文档生成、PPT 生成、多端协同、产物交付和归档摘要。
十三、项目卖点表达
答辩时不要说“我们写了一个 Python 脚本调几个工具”。这会显得很弱。
应该说：我们没有重造办公套件，而是基于飞书开放生态和 Codex + superpowers，构建了一个办公任务编排壳。系统将 IM 中的自然语言需求转化为 Agent 可执行的任务请求，由 Codex 在 superpowers 方法论约束下完成规划、澄清、子代理分工、工具调用和验收，再通过飞书云文档、演示稿、白板和 Presenton 等工具生成真实办公产物，最终回流到飞书多端协同环境中。
也可以说：我们的创新点不是单点生成文档或 PPT，而是把办公协作过程从“人手动跨应用搬运内容”转变为“用户表达目标，Agent 编排工具完成交付”。用户不再围绕软件按钮工作，而是围绕任务意图工作。
十四、阶段性实施计划
第一阶段先不接飞书，做本地 smoke test。准备 examples/demo_request.md，里面放模拟群聊和用户请求。让 Codex 读取这个请求，按项目 Skill 生成文档大纲、PPT 大纲、白板流程图和 artifacts.json。这个阶段验证 Codex + superpowers 是否能按照 Skill 和文件协议完成任务。
第二阶段接飞书入口，但只做最小能力。用户在飞书群里 @Agent，Python Bridge 收到消息，把任务写成 request.md，然后启动 Codex。任务完成后，Bridge 把 artifacts.json 中的链接回发飞书。
第三阶段加入 tmux 监控和 Textual 驾驶舱。开发者可以看到当前 Agent 运行状态、tmux 输出、任务列表、错误信息和产物链接。这样方便调试，也方便演示。
第四阶段加入更完整的飞书能力。比如读取群聊历史、创建或更新飞书文档、上传 PPTX、创建白板、生成分享链接、归档到知识库。
第五阶段再考虑高级加分项。比如语音输入、轻离线草稿、MCP Gateway、A2A 网关、多 Agent 可视化、冲突合并、白板布局编辑等。
十五、最终推荐路线
当前最推荐路线是：Codex + superpowers 做唯一主编排；飞书入口用 Python Bridge；办公工具用 lark-cli 和 lark-openapi-mcp；PPT 生成用 Presenton；运行监控用 tmux；开发者界面用 Textual；用户界面主要用飞书原生 IM、Doc、Slides 和 Whiteboard。
这条路线的优势是工作量小、对 TS 源码依赖低、充分利用 Codex 现有工程能力和 superpowers 方法论，贴合赛题“Agent 是 Pilot”的要求，也能在演示时表现出真实的多端办公闭环。
整个项目最关键的一句话是：不要改 Agent 框架内核，不要重造办公套件，而是把 Codex + superpowers 的办公编排约束写好，把飞书办公工具接好，把办公场景 Skill 写好，把产物回传协议定好。
